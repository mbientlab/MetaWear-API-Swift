import SwiftUI

extension Font {
    static let metric = Font.system(.title2, design: .rounded).weight(.semibold)
    static let metricCaption = Font.system(.caption, design: .rounded).weight(.medium)
}
